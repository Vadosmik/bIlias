services:
  app:
    build: 
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend-bilias
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev
    restart: always