services:
  backend:
    build: ./backend
    container_name: bllias-backend
    ports:
      - "8080:8000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    env_file:
      - backend/.env
    networks:
      - app-net
    restart: always
    depends_on:
      - db